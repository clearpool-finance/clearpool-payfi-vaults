{
 "repo": "Code/ozean/clearpool-payfi-vaults",
 "kind": "foundry",
 "changed_patterns": [
  "\\.sol$",
  "^foundry\\.toml$",
  "^remappings\\.txt$",
  "^script/",
  "^slither\\.config\\.json$"
 ],
 "gate": {
  "fast": [
   [
    "forge",
    "build"
   ]
  ],
  "heavy": [
   [
    "forge",
    "test",
    "--no-match-contract",
    "LiveDeploy"
   ]
  ],
  "fast_timeout_sec": 240,
  "heavy_timeout_sec": 600,
  "lock_deadline_sec": 1200
 },
 "evidence_dir": ".evidence/",
 "test_census": {
  "cmd": [
   "forge",
   "test",
   "--list",
   "--no-match-path",
   "test/LiveDeploy.t.sol"
  ],
  "count_pattern": "^\\s+(test|invariant_)"
 },
 "notes": "heavy excludes the LiveDeploy contract by name: it is a fork rehearsal whose constructor reads L1_RPC_URL via vm.envString and whose setUp needs a fresh deployment-config (E3), so it cannot run in a gate that never reads .env. `--no-match-path test/LiveDeploy.t.sol` did NOT exclude it (forge matches that option as a glob against the full path); the gate refused the push that exposed this. A stated exclusion, not a silent skip (harness plan C1/E3, 2026-09-05)."
}
